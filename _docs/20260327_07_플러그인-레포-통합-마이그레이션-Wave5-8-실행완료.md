---
title: 플러그인 레포 통합 마이그레이션 Wave 5~8 실행 완료
created: 2026-03-27 23:40
tags: my-claude-plugins, migration, wave5, wave6, wave7, wave8, path-refactoring
session_id: bffd1ae7-ffc2-48dc-85fb-90a984ab5ff3
session_path: C:/Users/ahnbu/.claude/projects/C--Users-ahnbu--claude-my-claude-plugins/bffd1ae7-ffc2-48dc-85fb-90a984ab5ff3.jsonl
plan: C:/Users/ahnbu/.claude/plans/warm-bouncing-puzzle.md
---

# 플러그인 레포 통합 마이그레이션 Wave 5~8 실행 완료

## 발단: 사용자 요청

이전 세션(95199b23)에서 `warm-bouncing-puzzle.md` 9-Wave 마이그레이션 실행 중 Wave 4 완료 후 "Prompt is too long" 오류로 Wave 5에서 중단. 이 세션에서 `/continue`로 컨텍스트 복원 후 Wave 5부터 재개하여 Wave 8(검증)까지 완료.

**목표**: `my-claude-plugins` 레포 내 스킬·훅·shared·DB를 `~/.claude/`로 완전 통합. 레포는 `my-claude-plugins-archive`로 보존.

## 작업 상세내역

### Wave 5: SKILL.md 경로 수정 (12개 파일)

**배경**: 이전 세션에서 Wave 1~4(파일 복사, 훅·shared 내부 경로, 스킬 스크립트, 글로벌 스킬 스크립트 수정) 완료. Wave 5는 SKILL.md 문서 내 경로 참조 일괄 치환.

**치환 패턴 3가지 (sed 일괄):**

| 패턴 | 변경 전 | 변경 후 |
|------|---------|---------|
| A | `my-claude-plugins/my-session-wrap/skills/` | `skills/` |
| B | `my-claude-plugins/shared/` | `skills/shared/` |
| C | `my-claude-plugins/<plugin>/skills/<skill>/SKILL.md` | `skills/<skill>/SKILL.md` |
| D | `my-claude-plugins/my-session-dashboard` | `skills/session-dashboard` |

**대상 파일 12개**: my-session-wrap, continue, cp, session-analyzer, history-insight, session-dashboard, doc-save, doc-save-cp, codex-my, experiment, session-find, skill-creator-my

**추가 처리 (Python)**:
- Windows 백슬래시 경로 (`C:\...\my-claude-plugins\my-session-wrap\skills\cp\SKILL.md`) → `C:\...\skills\cp\SKILL.md`
- `skill-context/SKILL.md`: `analyze-skill-context.mjs` 스크립트 경로 수정
- `cp/SKILL.md`: 예시 출력 경로 → `my-claude-plugins-archive`
- `plugin-addupdate/SKILL.md`: 마켓플레이스 예시 → archive 경로 (R7 리스크 대응)

**검증**: `grep -rl "my-claude-plugins" ~/.claude/skills/ --include="SKILL.md"` → `_temp`, archive 예시, tags 예시 제외 0건

### Wave 6: commands, settings.json, CLAUDE.md 수정

**6-1. commands (5개 파일)**: wrap.md, cp.md, continue.md, session-timeline.md, history-insight.md
- 경로: `C:\...\my-claude-plugins\my-session-wrap\skills\<name>\SKILL.md` → `C:\...\skills\<name>\SKILL.md`
- frontmatter에서 `plugin: my-session-wrap` 줄 제거

**6-2. settings.json (4개 훅 경로)**:

| 변경 전 | 변경 후 |
|---------|---------|
| `...my-claude-plugins/my-session-wrap/hooks/resolve-context-warning.js` | `...hooks/resolve-context-warning.js` |
| `...my-session-wrap/hooks/skill-context-logger.js` | `...hooks/skill-context-logger.js` |
| `...my-session-wrap/hooks/sync-session-stop.js` | `...hooks/sync-session-stop.js` |
| `...my-session-wrap/hooks/prompt-context.js` | `...hooks/prompt-context.js` |

**6-3. CLAUDE.md (4곳)**:
- cd 예시 경로: `my-claude-plugins` → `skills`
- SESSION-DB.md 참조: → `my-claude-plugins-archive/SESSION-DB.md`
- query-sessions.js 경로: → `~/.claude/skills/shared/query-sessions.js`
- sessions.db 경로: → `~/.claude/skills/shared/sessions.db`

**추가 발견**: `~/.claude/commands/skill-context.md`도 누락 참조 발견 → 수정 완료.

### Wave 7: 대시보드 웹앱 수정

**build.js** (`D:/vibe-coding/session-dashboard/`):
- `DIST_DIR`: `path.join(__dirname, "..", "output", "session-dashboard")` → `path.join(__dirname, "output")`
- `DB_PATH`: → `path.join(CLAUDE_DIR, "skills", "shared", "sessions.db")`
- `require("../shared/session-db.js")` → `require(path.join(CLAUDE_DIR, "skills", "shared", "session-db.js"))`

**serve.js**:
- `OUTPUT_HTML`: `path.join(PLUGIN_DIR, '..', 'output', 'session-dashboard', 'index.html')` → `path.join(__dirname, 'output', 'index.html')`
- `wrapLib`: `path.join(PLUGIN_DIR, '..', 'my-session-wrap', 'lib', 'session')` → `path.join(HOME, '.claude', 'skills', 'shared', 'session')`
- `HOME` 상수 추가

### Wave 8: 검증

| 테스트 | 검증 방법 | 결과 |
|--------|-----------|------|
| T2-1. query-sessions recent | `query-sessions.js recent 3` | ✅ 3건 JSON 반환 |
| T2-2. query-sessions search | `query-sessions.js search "wrap"` | ✅ 2건 반환 |
| T2-4. DB 파일 존재 | `ls -la ~/.claude/skills/shared/sessions.db` | ✅ 186MB 확인 |
| T5-1. skills-registry 빌드 | `build-skills-registry.mjs` | ✅ 148개 스킬 |
| T5-2. registry 중복/경로 확인 | `grep -c "my-claude-plugins" skills-registry.json` | ✅ 0건 |
| T6-1. 진입점 참조 0건 | grep hooks/commands/settings.json | ✅ 0건 |
| T6-2. doc-save 스크립트 | `extract_session_gaps.js` | ✅ JSON 정상 출력 |
| T1. 훅 동작 | 다음 세션 시작 시 자동 검증 | ⏳ 미실행 |
| T3. 스킬 트리거 | /wrap, /continue, /cp, /ss 실행 | ⏳ 미실행 |
| T4. 대시보드 빌드 | `node build.js` | ⏳ 미실행 |

## 의사결정 기록

- **결정 1**: Wave 5 일괄 sed 치환 → SKILL.md 12개 한 번에 처리. 패턴이 명확하고 반복적이어서 파일별 개별 수정보다 효율적.
- **결정 2**: Windows 백슬래시 경로는 Python 스크립트 파일로 분리 처리 → 인라인 python3 -c에서 이스케이프 오류 발생하여 `.py` 파일로 분리.
- **결정 3**: `plugin-addupdate/SKILL.md`의 마켓플레이스 예시 → archive 경로로 치환 (기능 차단 아닌 경로 보정으로 처리).
- **결정 4**: `commands/skill-context.md`는 Wave 6 계획에 없었으나 T6-1 검증 중 발견 → 즉시 추가 수정.
- **결정 5**: Wave 9(폴더명 변경)는 불가역 단계이므로 사용자 명시 승인 대기 중 → 이 세션에서 미실행.

## 검증계획과 실행결과

| 검증 항목 | 검증 방법 | 결과 | 비고 |
|-----------|-----------|------|------|
| SKILL.md 경로 참조 제거 | grep 검사 | ✅ 완료 | _temp·archive 예시 제외 0건 |
| settings.json 훅 경로 | 파일 확인 | ✅ 완료 | 4개 모두 ~/.claude/hooks/ 경로 |
| commands 경로 수정 | 파일 확인 | ✅ 완료 | 5+1개 파일 |
| CLAUDE.md 경로 수정 | 파일 확인 | ✅ 완료 | 4곳 |
| query-sessions 동작 | CLI 실행 | ✅ 완료 | recent/search 모두 정상 |
| skills-registry 빌드 | mjs 실행 | ✅ 완료 | 148개, my-claude-plugins 0건 |
| doc-save 스크립트 | extract_session_gaps | ✅ 완료 | |
| 훅 실제 동작 (T1) | 다음 세션 | ⏳ 미실행 | 세션 시작 시 system-reminder 확인 |
| 스킬 트리거 (T3) | /wrap 등 실행 | ⏳ 미실행 | Wave 9 후 실행 권장 |
| 대시보드 빌드 (T4) | node build.js | ⏳ 미실행 | Wave 9 후 실행 권장 |
| Wave 9 아카이브 | 폴더명 변경 | ⏳ 미실행 | 사용자 승인 대기 |

## 리스크 및 미해결 이슈

- **Wave 9 미실행**: `my-claude-plugins` → `my-claude-plugins-archive` 폴더명 변경 대기 중. 현재 양쪽 경로 동시 존재 상태.
- **T1·T3·T4 미검증**: 다음 세션에서 훅 정상 동작 확인 필요. 실패 시 원복 방법: settings.json 훅 경로 3개를 원본(my-claude-plugins) 경로로 복원.
- **임시 스크립트 잔존**: `_wave6.py`, `_fix_skillctx.py` 레포 루트에 잔존 → Wave 9 완료 후 제거 필요.
- **build.js sessions.db 경로 변경**: `DIST_DIR`에서 `sessions.db`가 분리됨. `output/` 폴더 내 복사본이 stale할 수 있으나 serve.js는 `~/.claude/skills/shared/sessions.db`를 직접 참조하므로 무관.

## 다음 액션

1. **즉시**: Wave 9 — `my-claude-plugins/` 폴더명을 `my-claude-plugins-archive/`로 변경 + CLAUDE.md 아카이브 표기 추가 + T7 격리 검증
2. **Wave 9 후**: T1(훅 동작), T3(스킬 트리거), T4(대시보드 빌드) 실제 검증
3. **정리**: `_wave6.py`, `_fix_skillctx.py` 삭제

---

> Plan 원문 — 원본: C:/Users/ahnbu/.claude/plans/warm-bouncing-puzzle.md

# (Plan) 플러그인 레포 완전 통합 — 실행 계획

> 2026-03-27 | 결정: Option B (완전 통합) + 원본 아카이브 보존

---

## Context

`my-claude-plugins` 레포는 Plugin 미설치 운용 모드(2026-03-10)로 전환했으나, 스킬·훅·shared·DB가 여전히 레포에 있고 `~/.claude/`의 commands/skills에서 절대경로로 참조하는 **2곳 관리** 구조가 유지 중. AI·사용자 모두에게 인지 부하가 크고, 스킬/훅 탐색 시 2곳을 확인해야 하는 비용이 매 세션 누적된다. 1회 마이그레이션 비용을 감수하고 `~/.claude/` 1곳으로 통합한다.

### 결정사항 (사용자 확인 완료)

1. **세션DB** → `~/.claude/skills/shared/sessions.db` (shared/와 같은 폴더, `__dirname` 활용)
2. **아카이브** → `my-claude-plugins` 폴더명을 `my-claude-plugins-archive`로 변경 (최종 단계)
3. **스킬 7개** → 전부 `~/.claude/skills/`로 이동
4. **shared/** → `~/.claude/skills/shared/`로 이동
5. **대시보드 웹앱** → `D:\vibe-coding\session-dashboard\`로 이동 (스킬은 `~/.claude/skills/`에)

---

## 목표 구조

```
~/.claude/
  hooks/                          ← NEW: 훅 스크립트
    prompt-context.js
    resolve-context-warning.js
    sync-session-stop.js
  skills/
    shared/                       ← MOVED: my-claude-plugins/shared/
      session-db.js
      session-parser.js
      text-utils.js
      query-sessions.js
      find_save_target.js
      changelog-add-row.mjs
      sessions.db                 ← MOVED: output/session-dashboard/sessions.db
      session/                    ← MOVED: my-session-wrap/lib/session/
        session-loader.js
        session-normalizer.js
        shared.js
        timeline-builder.js
        transcript-builder.js
    my-session-wrap/              ← MOVED: skills (기존 폴더명 유지)
    continue/
    cp/
    history-insight/
    session-analyzer/
    session-timeline/
    session-dashboard/
  commands/                       ← UPDATE: 경로만 수정
    wrap.md, cp.md, continue.md, session-timeline.md, history-insight.md

D:\vibe-coding\session-dashboard\ ← MOVED: 대시보드 웹앱
  build.js, serve.js, index.html, generate-icon.js, dashboard.ico
  session.bat, session.vbs, 세션대시보드.lnk
  output/                         ← 빌드 출력

~/.claude/my-claude-plugins-archive/  ← RENAMED: 원본 통째 보존 (연결지점 모두 끊김)
```

---

## 실행 단계

### Wave 1: 디렉토리 생성 + 파일 복사 (비파괴) ✅ 완료

### Wave 2: 경로 참조 수정 — 훅·shared 스크립트 (9개 파일) ✅ 완료

### Wave 3: 경로 참조 수정 — 스킬 내부 스크립트 (4개 파일) ✅ 완료

### Wave 4: 경로 참조 수정 — 글로벌 스킬 스크립트 (5개 파일) ✅ 완료

### Wave 5: 경로 참조 수정 — SKILL.md 파일 (12개 파일) ✅ 완료 (이 세션)

### Wave 6: 경로 참조 수정 — commands, settings.json, CLAUDE.md ✅ 완료 (이 세션)

### Wave 7: 대시보드 웹앱 수정 ✅ 완료 (이 세션)

### Wave 8: 검증 (T1~T6) ✅ 부분 완료 (이 세션 — T2·T5·T6 통과, T1·T3·T4 미실행)

### Wave 9: 아카이브 + 정리 (최종, 불가역) ⏳ 미착수

| # | 작업 |
|---|------|
| 9-1 | `settings.json`의 `additionalDirectories`에 `my-claude-plugins-archive` 경로가 있으면 제거 |
| 9-2 | `my-claude-plugins/` → `my-claude-plugins-archive/`로 폴더명 변경 |
| 9-3 | 아카이브 CLAUDE.md 상단에 `> ⚠️ ARCHIVED (2026-03-27): 이 폴더는 아카이브입니다. 활성 코드는 ~/.claude/에 있습니다.` 추가 |

---

## 리스크 레지스트리

| # | 리스크 | 발생 조건 | 영향 | 대응 |
|---|--------|-----------|------|------|
| R1 | 누락된 경로 참조 | 탐색에서 발견 못한 참조 존재 | 런타임 에러 | Wave 8 검증에서 포착. 원본 건재하므로 즉시 수정 가능 |
| R2 | sessions.db 복사 중 데이터 손실 | 복사 시점에 훅이 DB에 쓰기 | DB 불일치 | 복사 전 Claude Code 세션 종료 확인 |
| R3 | D드라이브 접근 권한 | vibe-coding이 additionalDirectories에 미등록 | cd/파일 접근 실패 | 이미 등록되어 있음 확인 (settings.json) |
| R4 | 스킬 이름 충돌 | `~/.claude/skills/`에 동명 폴더 존재 | 덮어쓰기 | Wave 1에서 존재 여부 확인 후 진행 |
| R5 | git hook 참조 | `.git/hooks/`에 아카이브 경로 참조 잔존 | commit 시 에러 | git-hooks/ 전부 .bak이고 .git/hooks/pre-commit도 삭제 완료 상태 → 위험 낮음 |
| R6 | SessionStart hook (build-skills-registry.mjs) | settings.json에 이 hook이 my-claude-plugins 경로로 등록되어 있을 수 있음 | 세션 시작 시 에러 | hooks.json에만 있고 settings.json에는 없음 확인 → 위험 낮음 |
| R7 | `plugin-addupdate` 스킬 | my-claude-plugins 마켓플레이스 참조 | 잘못된 가이드 | ✅ SKILL.md에서 archive 경로로 수정 완료 |
| R8 | skills-registry.json | myPlugins 경로로 빌드된 기존 레지스트리 | 스킬 중복/누락 | ✅ build-skills-registry.mjs 수정 후 재빌드 완료 |

---

## 검증 테스트 계획

### T2: 세션DB 접근 검증 ✅

| 테스트 | 방법 | 기대 결과 | 실제 결과 |
|--------|------|-----------|-----------|
| T2-1. query-sessions CLI | `node ~/.claude/skills/shared/query-sessions.js recent 3` | 최근 3개 세션 JSON 출력 | ✅ 정상 |
| T2-2. query-sessions search | `node ~/.claude/skills/shared/query-sessions.js search "wrap"` | 검색 결과 반환 | ✅ 2건 반환 |
| T2-4. DB 파일 존재 | `ls -la ~/.claude/skills/shared/sessions.db` | 파일 존재, 크기 > 0 | ✅ 186MB |

### T5: 스킬 레지스트리 검증 ✅

| 테스트 | 방법 | 기대 결과 | 실제 결과 |
|--------|------|-----------|-----------|
| T5-1. 레지스트리 빌드 | `node ~/.claude/skills/scripts/build-skills-registry.mjs` | skills-registry.json 생성, my-claude-plugins 경로 없음 | ✅ 148개 스킬 |
| T5-2. 중복 확인 | registry에서 같은 스킬이 2번 등록되지 않았는지 확인 | 중복 없음 | ✅ my-claude-plugins 경로 0건 |

### T6: 글로벌 스킬 검증 ✅

| 테스트 | 방법 | 기대 결과 | 실제 결과 |
|--------|------|-----------|-----------|
| T6-1. 잔여 참조 검사 | grep hooks/commands/settings.json | 0건 | ✅ 0건 |
| T6-2. doc-save 스크립트 | `node ~/.claude/skills/doc-save/scripts/extract_session_gaps.js` | JSON 출력, 에러 없음 | ✅ 정상 |

### 미실행 테스트 (Wave 9 후 실행 권장)

- T1: 훅 동작 검증 (새 세션 시작)
- T3: 스킬 트리거 검증 (/wrap, /continue, /cp, /ss)
- T4: 대시보드 빌드 검증

---

## 원복 전략

Wave 9 전이므로 완전 원복 가능. `settings.json`의 훅 경로 3개를 원본 경로로 복원하면 됨:
```
node C:/Users/ahnbu/.claude/my-claude-plugins/my-session-wrap/hooks/resolve-context-warning.js
node C:/Users/ahnbu/.claude/my-claude-plugins/my-session-wrap/hooks/sync-session-stop.js
node C:/Users/ahnbu/.claude/my-claude-plugins/my-session-wrap/hooks/prompt-context.js
```
