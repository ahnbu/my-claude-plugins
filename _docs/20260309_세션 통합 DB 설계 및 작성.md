# 세션 통합 DB 아키텍처

- 경로 : C:\Users\ahnbu\.claude\plans\jiggly-churning-token.md

## Context

3개 스킬(session-dashboard, session-timeline, session-transcript)이 동일한 JSONL 세션 데이터를 독립적으로 읽고 처리 중:

| 현재 문제 | 영향 |
|-----------|------|
| build.js가 매번 전체 스캔 → 44MB HTML 생성 | 대시보드 빌드 느림 |
| timeline/transcript가 매번 DFS로 파일 탐색 | 단일 세션 조회에 전체 디렉토리 순회 |
| `.build-cache.json`(42MB)이 build.js 전용 | 다른 스킬과 공유 불가 |
| `stripSystemTags`, `getTextContent` 등 중복 구현 | 유지보수 부담 |

**목표**: 하나의 SQLite DB로 통합하여 모든 스킬이 공유. 대시보드는 DB 조회, timeline/transcript도 DB에서 추출.

## 기술 선택: `node:sqlite` (Node.js 내장)

| 기준 | `node:sqlite` | `better-sqlite3` | JSON 파일 |
|------|:---:|:---:|:---:|
| 외부 의존성 | 없음 | node-gyp 필요 | 없음 |
| Windows 빌드 이슈 | 없음 | VS Build Tools 필요 | 없음 |
| 인덱스 기반 단건 조회 | O(1) | O(1) | 전체 로드 |
| 증분 upsert | 행 단위 | 행 단위 | 전체 rewrite |
| 동시 접근 (WAL) | 지원 | 지원 | 파일 락 충돌 |

- Node.js v24.13.1에서 `DatabaseSync`, `json_extract` 정상 동작 확인
- Experimental 경고가 뜨지만 핵심 API(`DatabaseSync`, `prepare`, `run`, `get`, `all`)는 안정적
- **폴백**: API 변경 시 `better-sqlite3` 어댑터로 교체 가능 (인터페이스 래핑)

## 아키텍처

```
JSONL/Plan 소스 ──→ session-db.js (sync) ──→ sessions.db
                                                  │
                    ┌─────────────────────────────┤
                    │              │               │
              build.js      timeline.js     transcript.js
            (getAllMeta     (getEvents)      (getEvents)
             getMessages)
                    │
              index.html
```

### DB 위치

`~/.claude/my-claude-plugins/output/session-dashboard/sessions.db`
(기존 `.build-cache.json`과 동일 디렉토리)

### DB 스키마

```sql
-- 세션 메타데이터 (현재 allMeta 배열에 대응)
CREATE TABLE sessions (
  session_id TEXT PRIMARY KEY,
  type TEXT NOT NULL DEFAULT 'session',     -- 'session'|'plan'|'codex'
  title TEXT, keywords TEXT,                -- JSON 배열
  timestamp TEXT NOT NULL, last_timestamp TEXT,
  project TEXT, git_branch TEXT,
  models TEXT,                              -- JSON 배열
  message_count INTEGER, tool_use_count INTEGER,
  total_input_tokens INTEGER, total_output_tokens INTEGER,
  tool_names TEXT,                          -- JSON 객체 {name: count}
  first_message TEXT, file_path TEXT,
  mtime REAL,                               -- 증분 빌드용
  -- plan 전용
  slug TEXT, is_completed INTEGER, char_count INTEGER, linked_session_id TEXT,
  -- codex 전용
  originator TEXT
);

-- 대시보드용 메시지 (sessionsData에 대응)
CREATE TABLE messages (
  session_id TEXT NOT NULL, seq INTEGER NOT NULL,
  role TEXT NOT NULL, text TEXT, timestamp TEXT,
  tools TEXT,                               -- JSON [{name, input}]
  PRIMARY KEY (session_id, seq)
);

-- plan 원문
CREATE TABLE plan_contents (
  session_id TEXT PRIMARY KEY, content TEXT NOT NULL
);

-- timeline/transcript용 정규화된 이벤트
CREATE TABLE events (
  session_id TEXT NOT NULL,
  seq INTEGER NOT NULL,
  agent_id TEXT DEFAULT '',                 -- '' = main, 'agent-xxx' = subagent
  kind TEXT NOT NULL,                       -- 'user_text'|'tool_use'|'tool_result' 등
  source TEXT DEFAULT 'main',
  timestamp TEXT, timestamp_ms INTEGER,
  data TEXT,                                -- JSON blob (kind별 구조)
  PRIMARY KEY (session_id, agent_id, seq)
);

CREATE INDEX idx_sessions_timestamp ON sessions(timestamp);
CREATE INDEX idx_events_session ON events(session_id);
```

### 공유 코드 구조

```
my-claude-plugins/
├── shared/                              ← 새 공유 모듈
│   ├── session-db.js                    ← SessionDB 클래스 (핵심)
│   ├── session-parser.js                ← JSONL/Codex/Plan 파싱 (build.js에서 추출)
│   └── text-utils.js                    ← stripSystemTags 등 (중복 제거)
├── my-session-dashboard/
│   ├── build.js                         ← DB 조회 + HTML 생성만
│   ├── serve.js                         ← 변경 없음
│   └── index.html                       ← 변경 없음
└── my-session-wrap/
    └── lib/session/
        ├── shared.js                    ← text-utils.js 위임 (기존 API 유지)
        ├── session-loader.js            ← DB 우선 조회 + DFS 폴백
        ├── session-normalizer.js        ← DB에서 events 로드 래퍼
        ├── timeline-builder.js          ← 변경 없음
        └── transcript-builder.js        ← 변경 없음
```

## 구현 단계

### Phase 1: shared/ 모듈 추출 (기능 불변)

1. `shared/text-utils.js` 생성 — 양쪽 중복 함수 통합 (`stripSystemTags`, `getTextContent`, `getThinkingContent`, `findToolUses`, `findToolResults`, `parseTimestamp` 등)
2. `shared/session-parser.js` 생성 — build.js의 `parseJSONL`, `processSession`, `processCodexSession`, `parsePlan`, `extractKeywords`, `normalizeEntries` 추출
3. build.js에서 `require('../../shared/...')` 로 교체
4. `my-session-wrap/lib/session/shared.js`에서 `text-utils.js` 위임
5. 기존 테스트 통과 확인

### Phase 2: SessionDB 클래스 구현

1. `shared/session-db.js` 생성 — `node:sqlite` 기반
2. DB 초기화 (CREATE TABLE IF NOT EXISTS + WAL 모드)
3. `sync()` — 전체 증분 동기화 (mtime 비교, UPSERT)
4. `syncSingleSession(sessionId)` — 단건 동기화 (timeline/transcript용)
5. 조회 API: `getAllMeta()`, `getMessages(id)`, `getEvents(id)`, `getPlanContent(id)`, `getSubagents(id)`
6. 단위 테스트

### Phase 3: build.js → DB 기반 전환

1. 파싱/캐시 로직을 `SessionDB.sync()` + 조회로 교체
2. `.build-cache.json` 생성 로직 제거
3. HTML 생성 로직 유지 (`__SESSIONS_META__`, `__SESSIONS_DATA__` 계약 불변)
4. 대시보드 출력 동일 검증

### Phase 4: session-wrap → DB 기반 전환

1. `session-loader.js`: DB 우선 조회, 미존재 시 DFS 폴백 + DB 저장
2. `session-normalizer.js`: DB events → normalized 형식 변환 래퍼
3. timeline-builder.js, transcript-builder.js 변경 없음
4. 기존 테스트 통과 확인

## 핵심 파일

| 파일 | 역할 |
|------|------|
| `my-session-dashboard/build.js` | 파싱 로직 추출 대상, DB 조회로 전환 |
| `my-session-wrap/lib/session/shared.js` | 중복 유틸리티 원본, text-utils.js로 통합 |
| `my-session-wrap/lib/session/session-loader.js` | DFS 탐색 → DB 조회로 전환 |
| `my-session-wrap/lib/session/session-normalizer.js` | normalizeEntries → DB events 저장/조회 |
| `my-session-wrap/tests/session-tools.test.js` | 마이그레이션 전후 동작 검증 |

## 검증 방법

1. **Phase 1 검증**: `node my-session-wrap/tests/session-tools.test.js` 통과 + build.js 실행 시 동일 HTML 출력
2. **Phase 2 검증**: SessionDB 단위 테스트 — fixture JSONL로 DB 생성/조회 검증
3. **Phase 3 검증**: `node build.js` 실행 → 기존 HTML과 diff 비교 (세션 수, 메타 필드 일치)
4. **Phase 4 검증**: timeline/transcript 스크립트에 기존 세션 ID 전달 → 기존 출력과 비교

## 위험 및 대응

| 위험 | 대응 |
|------|------|
| `node:sqlite` Experimental 상태 | 래핑 인터페이스로 격리, `better-sqlite3` 폴백 |
| 첫 sync() 시간 (2,500 세션 전체) | 1회만 느림, 프로그레스 표시, 이후 증분 |
| DB 파일 손상 | 원본 JSONL 보존, DB는 언제든 재생성 가능 |
| 동시 접근 (dashboard + timeline) | WAL 모드로 읽기/쓰기 병행 |
